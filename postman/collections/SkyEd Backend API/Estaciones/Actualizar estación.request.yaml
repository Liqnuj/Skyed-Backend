$kind: http-request
url: "{{base_url}}/estaciones/1"
method: PUT
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "nombre_est": "Estación de hidratación km 20 - actualizada"
    }
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 4000
