$kind: http-request
url: "{{base_url}}/pagos/1/estado"
method: PATCH
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "estado_p": "aprobado"
    }
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 3000
