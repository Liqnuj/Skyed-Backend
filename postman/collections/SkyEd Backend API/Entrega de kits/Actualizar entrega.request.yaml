$kind: http-request
url: "{{base_url}}/entregas-kit/1"
method: PUT
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "entregado_por_ek": "Staff punto 2"
    }
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 4000
