$kind: http-request
url: "{{base_url}}/eventos/1/categorias"
method: POST
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "nombre_cat": "Élite Masculino",
      "descripcion_cat": "Categoría profesional masculina"
    }
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 2000
