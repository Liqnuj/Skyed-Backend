$kind: http-request
url: "{{base_url}}/ambientes/1"
method: PUT
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "capacidad_amb": 180
    }
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 4000
