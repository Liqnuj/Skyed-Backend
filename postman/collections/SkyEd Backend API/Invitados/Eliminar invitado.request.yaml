$kind: http-request
description: Falla con 409 si el invitado sigue vinculado a un usuario.
url: "{{base_url}}/invitados/1"
method: DELETE
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 5000
