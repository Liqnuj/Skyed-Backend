$kind: http-request
url: "{{base_url}}/resultados/1"
method: PUT
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "posicion_r": 2
    }
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 4000
