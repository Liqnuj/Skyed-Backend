$kind: http-request
url: "{{base_url}}/qr/validar"
method: POST
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "codigo_qr": "PEGA_AQUI_EL_CODIGO"
    }
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 2000
