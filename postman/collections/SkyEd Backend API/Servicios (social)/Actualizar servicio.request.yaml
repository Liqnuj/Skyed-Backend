$kind: http-request
url: "{{base_url}}/servicios/1"
method: PUT
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "precio_serv": 55000
    }
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 4000
