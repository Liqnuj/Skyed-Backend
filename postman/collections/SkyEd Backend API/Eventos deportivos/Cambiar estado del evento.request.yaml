$kind: http-request
url: "{{base_url}}/eventos/1/estado"
method: PATCH
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "estado_e": "cerrado"
    }
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 5000
